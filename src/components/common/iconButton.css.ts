import { style } from '@vanilla-extract/css';

export const iconButton = style({
  padding: '6px',
  borderRadius: '100%',
});

export const iconButtonIcon = style({
  width: '24px',
  height: '24px',
});
